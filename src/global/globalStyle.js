import { RFPercentage, RFValue } from 'react-native-responsive-fontsize';

const colors = {
    warning: "yellow",
    primary: "#E75935",
    danger: 'red',
    success: 'rgba(32, 32, 32, 0.5)'
}

const size = {
    heading: 20,
    title: 18,
    subtitle: 16,
    label: 14,
}

const fontfamily = {
    bold: 'Mula-Bold',
    regular: 'Mula-Regular',
    medium: 'Mula-Medium',
    light: 'Mula-Light',
}

const images = {
    logo: require('../images/logoadmin.png'),
    bg : require('../images/bg.jpeg'),
    menu : require('../images/menu.png'),

  
}


export { colors, size, fontfamily, images }